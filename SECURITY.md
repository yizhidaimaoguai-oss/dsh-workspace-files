# 安全说明

本插件拥有对所选远程工作目录的文件读写能力。只在信任的 DSH 部署中安装，并使用 DSH 原有登录机制。

## 已实施的限制

- 路径必须在登记的工作目录中，拒绝目录穿越、符号链接与特殊文件。
- Linux 打开的文件描述符再次进行路径校验。
- 保存必须携带读取时的版本；拒绝硬链接写入及过期版本。
- 文本和下载有大小限制，目录列表有条目限制。
- 浏览器变更请求要求同源及专用请求头。
- 配对票据仅使用一次，60 秒到期；客户端凭据按工作目录隔离，默认 30 天到期。
- 凭据保存在 VSCode SecretStorage；服务端只持久化散列，POSIX 授权文件默认权限 0600；Windows 使用系统目录 ACL，mode 参数不等价于访问控制。
- 非回环 HTTP 连接被扩展拒绝；禁止 HTTP 重定向携带桌面凭据。

## 限制

没有完整的审计日志、速率限制系统或多用户权限模型。授权文件属于运行 DSH 的系统用户；工作目录本身如果包含秘密文件，获得授权的客户端能够读取这些文件。

提供此说明不代表经过独立安全审计。SecretStorage 的保护程度取决于 VSCode 和本机操作系统。

## 报告问题

不要在公开 Issue 中提交有效凭据、带 ticket 的链接、DSH 启动登录链接或个人文件内容。若仓库启用了 GitHub Private Vulnerability Reporting，优先使用该入口；否则只提交不含敏感细节的联系请求，再与维护者约定私下渠道。

怀疑凭据泄露时，立即从 DSH 网页“客户端授权”撤销相关授权，再重新配对。

## 跨平台文件系统边界

Linux 保留文件描述符目录锚定。Windows/macOS 使用逐段 lstat、realpath 与文件身份重检，并拒绝符号链接、目录联接及 Windows 特殊路径。这不是操作系统沙箱；不承诺抵御有本机目录写权限的敌对进程反复交换路径的竞态。请勿将敌对多用户可写目录作为授权根目录。

原子替换保存保留普通 POSIX mode；不保留所有者、ACL、扩展属性、资源分叉或 Windows 单文件自定义 ACL。新文件继承父目录的系统权限，精细权限可能变化；只适合普通工作区文件。无需卸载安全检查来启用其他系统。

Node.js 的文件打开标志和权限位具有平台差异，参考 [Node.js 文件系统文档](https://nodejs.org/api/fs.html#file-system-flags)。
